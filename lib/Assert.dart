//Syntax.......

/*assert(Condition);
//or
assert(Condition, "Your custom message");*/

/*Meaning = It is hard to find errors in big projects so dart 
provide a assert method to check for the error. */
void main() {
  int age = 22;
  assert(age == 56);
}
